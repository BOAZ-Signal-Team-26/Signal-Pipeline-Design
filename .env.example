# 인증키 템플릿. 실제 값은 .env 에 넣는다 (.gitignore 처리됨).
#   cp .env.example .env  후 값을 채운다
# 공공데이터포털 (data.go.kr) 일반 인증키 — Decoding 값을 쓴다
DATA_GO_KR_SERVICE_KEY=
# KRX Open API (openapi.krx.co.kr) 인증키
KRX_AUTH_KEY=
